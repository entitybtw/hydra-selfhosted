JWT_SECRET=change-me-to-random-string
PORT=3000
# Instance token — put this in Hydra Settings → Self-Hosted API → Instance Token
INSTANCE_TOKEN=change-me-instance-token
# API token for web profile access (/u/username)
API_TOKEN=
