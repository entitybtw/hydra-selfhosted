services:
  api:
    build: ./api
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - ./data:/data
    environment:
      - API_TOKEN=${API_TOKEN:-change-me}
      - PORT=${PORT:-3000}
    restart: unless-stopped
